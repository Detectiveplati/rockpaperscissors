console.log("Testing123")

// Make a function that gets a 
function computerChoice() {
    
let number = Math.floor(Math.random() * 3) + 1;
if (number === 1)
    return "Scissors";
else if (number === 2)
    return "Rock";
else (number === 3)
    return "Paper"
}

console.log(computerChoice());
// Tag a number to scissors, papers and stone

// Make the computerChoice function 

// Make the userChoice function

// Match the user function against the computer and decide who wins